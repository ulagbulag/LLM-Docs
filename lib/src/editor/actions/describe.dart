import 'package:flutter/material.dart';

import 'base.dart';

final class EditorActionDescribeView extends EditorActionView {
  const EditorActionDescribeView({
    super.key,
  });

  @override
  String title(BuildContext context) => 'a';
}
